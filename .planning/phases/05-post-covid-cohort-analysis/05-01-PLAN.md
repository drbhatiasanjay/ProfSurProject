---
phase: 05-post-covid-cohort-analysis
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - graph_builder.py
  - tests/test_covid_cohorts.py
autonomous: true

must_haves:
  truths:
    - "compute_covid_cohorts() returns a profitability_change column in cohort_df"
    - "test_covid_cohorts.py covers: structure, count consistency, leverage_change, profitability_change, entered_decline, recovered, and statistical test guard"
    - "All existing 344+ tests still pass after the graph_builder.py edit"
  artifacts:
    - path: "graph_builder.py"
      provides: "profitability_change column in compute_covid_cohorts return value"
      contains: "profitability_change"
    - path: "tests/test_covid_cohorts.py"
      provides: "Unit test module for compute_covid_cohorts"
      contains: "test_cohort_result_structure"
  key_links:
    - from: "compute_covid_cohorts() rows.append()"
      to: "profitability_change key"
      via: "post_prof - pre_prof arithmetic"
      pattern: "profitability_change"
---

<objective>
Add profitability_change to compute_covid_cohorts() and write a comprehensive unit test module tests/test_covid_cohorts.py covering all four success criteria that the UI will consume in wave 2.

Purpose: The existing function computes pre_profitability and post_profitability but never packages the delta. The UI layer (05-02) needs profitability_change to show COH-03 comparison. Tests must confirm the function is stable before the UI layer is built.
Output: Updated graph_builder.py (one-line addition) + new tests/test_covid_cohorts.py (7-10 tests).
</objective>

<execution_context>
@C:\Users\hemas\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\hemas\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@graph_builder.py
@helpers.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add profitability_change column to compute_covid_cohorts()</name>
  <files>graph_builder.py</files>
  <action>
In `graph_builder.py`, find the `compute_covid_cohorts()` function (around line 600). Inside the `rows.append({...})` call (around line 659), there is already:

```python
"leverage_change": (post_lev - pre_lev) if pre_lev is not None and post_lev is not None else None,
```

Immediately after that line, add:

```python
"profitability_change": (post_prof - pre_prof) if pre_prof is not None and post_prof is not None else None,
```

This follows the identical pattern of the leverage_change line directly above it. No other changes needed in this function.
  </action>
  <verify>grep -n "profitability_change" graph_builder.py</verify>
  <done>"profitability_change" appears in graph_builder.py inside the rows.append() call of compute_covid_cohorts()</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/test_covid_cohorts.py</name>
  <files>tests/test_covid_cohorts.py</files>
  <action>
Create `tests/test_covid_cohorts.py` with the following content. The test module uses the live thesis DB (same pattern as tests/test_cfo_graph.py — scope="module" fixtures for expensive graph build).

```python
"""
Unit tests for compute_covid_cohorts() in graph_builder.py.

Covers the four Phase 5 success criteria:
  COH-01: Post-COVID decline cohort identified and separated
  COH-02: COVID resilience tracker (improved vs deteriorated)
  COH-03: Leverage and profitability comparison columns present
  SC-4:   Cohort data accessible (function returns valid structure)
"""

import pytest
import sys
import os

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import db
from graph_builder import build_knowledge_graph, compute_covid_cohorts


# ── Fixtures ──────────────────────────────────────────────────────────────────

@pytest.fixture(scope="module")
def fin_df():
    """Full financial data for graph construction — thesis panel."""
    return db.get_graph_financials()


@pytest.fixture(scope="module")
def own_df():
    """Ownership data for graph construction."""
    return db.get_graph_ownership()


@pytest.fixture(scope="module")
def G(fin_df, own_df):
    """Full knowledge graph — built once per module."""
    return build_knowledge_graph(fin_df, own_df)


@pytest.fixture(scope="module")
def cohort_result(G, fin_df):
    """compute_covid_cohorts result — built once per module."""
    return compute_covid_cohorts(G, fin_df)


@pytest.fixture(scope="module")
def cohort_df(cohort_result):
    """The cohort DataFrame from the result."""
    return cohort_result["cohort_df"]


# ── Structure tests (SC-4: function returns valid structure) ──────────────────

class TestCohortStructure:

    def test_no_error_with_thesis_data(self, cohort_result):
        """Thesis panel has 2019 and 2022-2024 data — should never return error key."""
        assert "error" not in cohort_result, f"Unexpected error: {cohort_result.get('error')}"

    def test_required_keys_present(self, cohort_result):
        required = ["cohort_df", "n_total", "n_deteriorated", "n_improved",
                    "n_entered_decline", "n_recovered", "pct_deteriorated", "pct_improved"]
        for key in required:
            assert key in cohort_result, f"Missing key: {key}"

    def test_n_total_reasonable(self, cohort_result):
        """Thesis panel (401 firms, 2001-2024) should have 100+ firms with both 2019 and 2022+ obs."""
        assert cohort_result["n_total"] >= 50, \
            f"Expected at least 50 firms with pre/post COVID data, got {cohort_result['n_total']}"

    def test_required_columns_in_cohort_df(self, cohort_df):
        required_cols = [
            "company", "industry", "pre_stage", "post_stage",
            "leverage_change", "profitability_change",
            "deteriorated", "improved", "entered_decline_after_covid", "recovered",
        ]
        for col in required_cols:
            assert col in cohort_df.columns, f"Missing column in cohort_df: {col}"


# ── Count consistency tests ────────────────────────────────────────────────────

class TestCohortCountConsistency:

    def test_n_deteriorated_matches_df(self, cohort_result, cohort_df):
        assert cohort_result["n_deteriorated"] == int(cohort_df["deteriorated"].sum())

    def test_n_improved_matches_df(self, cohort_result, cohort_df):
        assert cohort_result["n_improved"] == int(cohort_df["improved"].sum())

    def test_n_entered_decline_matches_df(self, cohort_result, cohort_df):
        assert cohort_result["n_entered_decline"] == int(cohort_df["entered_decline_after_covid"].sum())

    def test_n_recovered_matches_df(self, cohort_result, cohort_df):
        assert cohort_result["n_recovered"] == int(cohort_df["recovered"].sum())

    def test_pct_deteriorated_in_range(self, cohort_result):
        assert 0.0 <= cohort_result["pct_deteriorated"] <= 100.0

    def test_pct_improved_in_range(self, cohort_result):
        assert 0.0 <= cohort_result["pct_improved"] <= 100.0


# ── COH-01: Post-COVID decline cohort identified ──────────────────────────────

class TestPostCovidDeclineCohort:

    def test_entered_decline_after_covid_column_exists(self, cohort_df):
        """COH-01: Column must exist and be boolean-like."""
        assert "entered_decline_after_covid" in cohort_df.columns
        assert cohort_df["entered_decline_after_covid"].dtype in (bool, object, "bool")

    def test_entered_decline_firms_were_not_in_decline_pre_covid(self, cohort_df):
        """COH-01: Firms that entered Decline after COVID must NOT have been in Decline/Decay in 2019."""
        decline_stages = {"Decline", "Decay", "Shakeout1", "Shakeout2", "Shakeout3"}
        entered = cohort_df[cohort_df["entered_decline_after_covid"]]
        if not entered.empty:
            assert not entered["pre_stage"].isin(decline_stages).any(), \
                "Firms flagged as entered_decline_after_covid must have non-decline pre_stage"

    def test_entered_decline_firms_are_in_decline_post_covid(self, cohort_df):
        """COH-01: Firms that entered Decline after COVID must be in Decline/Decay/Shakeout in 2022+."""
        decline_stages = {"Decline", "Decay", "Shakeout1", "Shakeout2", "Shakeout3"}
        entered = cohort_df[cohort_df["entered_decline_after_covid"]]
        if not entered.empty:
            assert entered["post_stage"].isin(decline_stages).all(), \
                "All entered_decline_after_covid firms must have decline-class post_stage"


# ── COH-02: COVID resilience tracker ─────────────────────────────────────────

class TestCovidResilienceTracker:

    def test_resilience_cohorts_are_mutually_exclusive(self, cohort_df):
        """COH-02: A firm cannot be both improved and deteriorated."""
        both = cohort_df[cohort_df["improved"] & cohort_df["deteriorated"]]
        assert both.empty, f"{len(both)} firms flagged as both improved and deteriorated"

    def test_recovered_firms_were_in_decline_pre_covid(self, cohort_df):
        """COH-02: Recovered firms must have been in decline stages pre-COVID."""
        decline_stages = {"Decline", "Decay", "Shakeout1", "Shakeout2", "Shakeout3"}
        recovered = cohort_df[cohort_df["recovered"]]
        if not recovered.empty:
            assert recovered["pre_stage"].isin(decline_stages).all(), \
                "All recovered firms must have had decline-class pre_stage"

    def test_recovered_firms_are_not_in_decline_post_covid(self, cohort_df):
        """COH-02: Recovered firms must NOT be in decline stages post-COVID."""
        decline_stages = {"Decline", "Decay", "Shakeout1", "Shakeout2", "Shakeout3"}
        recovered = cohort_df[cohort_df["recovered"]]
        if not recovered.empty:
            assert not recovered["post_stage"].isin(decline_stages).any(), \
                "Recovered firms must not have decline-class post_stage"


# ── COH-03: Leverage and profitability comparison columns ─────────────────────

class TestCohortMetricColumns:

    def test_leverage_change_has_values(self, cohort_df):
        """COH-03: leverage_change must have non-null values for most firms."""
        assert cohort_df["leverage_change"].notna().sum() >= 20

    def test_profitability_change_has_values(self, cohort_df):
        """COH-03: profitability_change must have non-null values (requires Task 1 edit)."""
        assert cohort_df["profitability_change"].notna().sum() >= 20

    def test_leverage_change_is_numeric(self, cohort_df):
        import pandas as pd
        assert pd.to_numeric(cohort_df["leverage_change"].dropna(), errors="coerce").notna().all()

    def test_profitability_change_is_numeric(self, cohort_df):
        import pandas as pd
        assert pd.to_numeric(cohort_df["profitability_change"].dropna(), errors="coerce").notna().all()

    def test_scipy_ttest_runs_on_cohorts(self, cohort_df):
        """COH-03: Welch's t-test must run without error when cohorts are large enough."""
        from scipy import stats
        det = cohort_df[cohort_df["deteriorated"]]["leverage_change"].dropna()
        imp = cohort_df[cohort_df["improved"]]["leverage_change"].dropna()
        if len(det) >= 5 and len(imp) >= 5:
            t_stat, p_val = stats.ttest_ind(det, imp, equal_var=False)
            assert p_val >= 0.0
            assert p_val <= 1.0

    def test_mannwhitneyu_runs_on_cohorts(self, cohort_df):
        """COH-03: Mann-Whitney U test must run without error when cohorts are large enough."""
        from scipy import stats
        det = cohort_df[cohort_df["deteriorated"]]["leverage_change"].dropna()
        imp = cohort_df[cohort_df["improved"]]["leverage_change"].dropna()
        if len(det) >= 5 and len(imp) >= 5:
            u_stat, p_mw = stats.mannwhitneyu(det, imp, alternative="two-sided")
            assert p_mw >= 0.0
            assert p_mw <= 1.0
```
  </action>
  <verify>py -3.12 -m pytest tests/test_covid_cohorts.py -v --tb=short 2>&1 | tail -30</verify>
  <done>All tests in test_covid_cohorts.py pass; output shows "passed" for every test class</done>
</task>

<task type="auto">
  <name>Task 3: Full suite gate</name>
  <files>none</files>
  <action>
Run the full test suite to confirm no regressions from the graph_builder.py edit.

```bash
py -3.12 -m pytest tests/ -v --tb=short -q 2>&1 | tail -20
```

Expected: 344+ passed (or current count + new test_covid_cohorts.py tests), 0 errors.
  </action>
  <verify>py -3.12 -m pytest tests/ -q 2>&1 | tail -5</verify>
  <done>All existing tests still pass plus new test_covid_cohorts.py tests; total count >= 344</done>
</task>

</tasks>

<verification>
1. `grep -n "profitability_change" graph_builder.py` returns a match inside compute_covid_cohorts rows.append()
2. `py -3.12 -m pytest tests/test_covid_cohorts.py -v 2>&1 | tail -20` shows all tests passed
3. `py -3.12 -m pytest tests/ -q 2>&1 | tail -5` shows 344+ passed, 0 errors
</verification>

<success_criteria>
- graph_builder.py compute_covid_cohorts() returns cohort_df with profitability_change column
- tests/test_covid_cohorts.py exists and all tests pass
- All 344+ pre-existing tests continue to pass
- Test module covers all four success criteria (COH-01, COH-02, COH-03, SC-4)
</success_criteria>

<output>
After completion, create `.planning/phases/05-post-covid-cohort-analysis/05-01-SUMMARY.md`
</output>
