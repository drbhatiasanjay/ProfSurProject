---
phase: 04-advanced-econometrics-page
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/13_advanced_econometrics.py
autonomous: true

must_haves:
  truths:
    - "pages/13_advanced_econometrics.py imports and calls run_system_gmm, run_delta_leverage_all, run_delta_leverage_by_stage, run_stage_comparison, run_iv_regression"
    - "All four tabs (System GMM, Delta-Leverage, Stage Comparisons, IV/2SLS) render without import errors"
    - "Every model result section is followed by a render_interpretation() call"
    - "Every st.dataframe() in the file is immediately followed by df_download_button()"
    - "run_breusch_pagan_lm result is surfaced in the UI (Tab 1 or Tab 2, after the model runs)"
    - "372+ tests pass with no regressions"
  artifacts:
    - path: "pages/13_advanced_econometrics.py"
      provides: "Fully working Advanced Econometrics page with GMM, delta-leverage, BP-LM display, stage comparisons, IV/2SLS"
      contains: "run_breusch_pagan_lm"
  key_links:
    - from: "Tab 1 System GMM button result"
      to: "render_interpretation(insights, actions)"
      via: "direct call after diagnostics block"
      pattern: "render_interpretation"
    - from: "Tab 2 Full Panel run_delta_leverage_all result"
      to: "run_breusch_pagan_lm(result['ols'])"
      via: "new code block after Hausman info line"
      pattern: "run_breusch_pagan_lm"
---

<objective>
Page 13 "Advanced Econometrics" is already implemented with all four tabs and all required render_interpretation() and df_download_button() calls. The only genuine gap vs. the success criteria is that `run_breusch_pagan_lm` is imported but its output is never surfaced in the UI. This plan adds a minimal BP-LM output display to Tab 2 (after the Hausman test row in Full Panel mode) and verifies the full page end-to-end.

Output: Updated `pages/13_advanced_econometrics.py` with BP-LM result shown in Tab 2 Full Panel mode. All 372+ tests continue to pass.
</objective>

<execution_context>
@C:\Users\hemas\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\hemas\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/04-advanced-econometrics-page/04-RESEARCH.md
@pages/13_advanced_econometrics.py
@models/econometric.py
@helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BP-LM display to Tab 2 Full Panel mode</name>
  <files>pages/13_advanced_econometrics.py</files>
  <action>
Read `pages/13_advanced_econometrics.py` fully before editing.

**Background:** `run_breusch_pagan_lm` is already imported at the top of the file (line 19). It tests whether Pooled OLS is adequate vs Random Effects (the LM statistic). It belongs naturally in Tab 2 Full Panel mode after the Hausman test result, since the user already sees OLS/FE/RE comparison there.

**What `run_breusch_pagan_lm` returns** (from models/econometric.py):
```python
{
    "lm_stat": float,      # LM test statistic
    "p_value": float,      # p-value
    "verdict": str         # plain-English verdict string
}
```

**Where to insert:** In Tab 2, inside the `if delta_mode == "Full Panel":` branch, AFTER the existing `st.info(f"Hausman test: chi2=...")` line, add:

```python
            # BP-LM Test: Pooled OLS vs Random Effects
            with st.spinner("Running Breusch-Pagan LM test..."):
                bplm = run_breusch_pagan_lm(result["ols"])
            st.info(
                f"Breusch-Pagan LM: statistic={bplm['lm_stat']:.4f}, "
                f"p={format_pvalue(bplm['p_value'])} — {bplm['verdict']}"
            )
```

This runs immediately after the button is clicked (same spinner context), uses already-imported `format_pvalue`, and keeps the display compact with `st.info` matching the Hausman line above it.

Do not change anything else in the file.

After editing, verify syntax:
`python -c "import ast; ast.parse(open('pages/13_advanced_econometrics.py').read()); print('syntax OK')"`
  </action>
  <verify>
python -c "import ast; ast.parse(open('C:/Users/hemas/Downloads/ProfSurProject/pages/13_advanced_econometrics.py').read()); print('syntax OK')"
grep -n "run_breusch_pagan_lm" pages/13_advanced_econometrics.py
Expected: syntax OK; at least 2 lines matching run_breusch_pagan_lm (import line + call site)
  </verify>
  <done>pages/13_advanced_econometrics.py calls run_breusch_pagan_lm inside Tab 2 Full Panel mode and shows the result with st.info(); file passes AST syntax check</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and confirm no regressions</name>
  <files></files>
  <action>
Run the full test suite to confirm all existing tests still pass:

```
py -3.12 -m pytest tests/ -q 2>&1 | tail -10
```

Expected: 372+ passed, 0 failed. If any test fails, read the failure message and fix the root cause before proceeding.

Also run the Page 13 tests specifically to confirm all 5 pass:
```
py -3.12 -m pytest tests/test_page_integration.py::TestPage13AdvancedEconometrics -v 2>&1
```

Expected: 5 passed.
  </action>
  <verify>
py -3.12 -m pytest tests/test_page_integration.py::TestPage13AdvancedEconometrics -v 2>&1 | tail -10
py -3.12 -m pytest tests/ -q 2>&1 | tail -5
Expected: TestPage13AdvancedEconometrics — 5 passed; overall — 372+ passed, 0 failed
  </verify>
  <done>All TestPage13AdvancedEconometrics tests pass; full test suite passes with 372+ tests and no regressions</done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('pages/13_advanced_econometrics.py').read()); print('OK')"` → OK
2. `grep -c "run_breusch_pagan_lm" pages/13_advanced_econometrics.py` → >= 2 (import + call)
3. `grep -c "render_interpretation" pages/13_advanced_econometrics.py` → >= 4 (one per tab)
4. `grep -c "df_download_button" pages/13_advanced_econometrics.py` → >= 7 (one per coefficient table)
5. `grep -c "chart_download_button" pages/13_advanced_econometrics.py` → >= 1 (Stage Comparison chart)
6. `py -3.12 -m pytest tests/test_page_integration.py::TestPage13AdvancedEconometrics -v 2>&1 | tail -3` → 5 passed
7. `py -3.12 -m pytest tests/ -q 2>&1 | tail -3` → 372+ passed, 0 failed
</verification>

<success_criteria>
- pages/13_advanced_econometrics.py loads without import errors (all 7 imported functions exist in models/econometric.py)
- All 4 tabs render: System GMM, Delta-Leverage, Stage Comparisons, IV/2SLS
- BP-LM result is shown in Tab 2 Full Panel mode after the Hausman test line
- Every model output section is followed by render_interpretation()
- Every st.dataframe() is followed by df_download_button()
- The Stage Comparison chart is followed by chart_download_button()
- All 5 TestPage13AdvancedEconometrics tests pass
- Full test suite: 372+ passed, 0 failed
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-econometrics-page/04-01-SUMMARY.md`
</output>
