---
phase: 01-delta-leverage-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_models.py
autonomous: true

must_haves:
  truths:
    - "Breusch-Pagan LM test result is asserted to contain chi-sq stat, p-value, and a non-empty verdict string (TST-01, TST-02)"
    - "Delta-leverage OLS/FE/RE test asserts first-differenced n_obs is strictly less than the input panel n_obs (DLV-01)"
    - "Delta-leverage hausman result is asserted to expose chi2, p_value, verdict, and a recommended field that drives selection (DLV-02)"
    - "Stage-specific delta-leverage test asserts at least one stage returns a coef_table and Shakeout1 returns the documented error path (DLV-03, DLV-04)"
  artifacts:
    - path: "tests/test_models.py"
      provides: "Hardened assertions on existing 4 delta-leverage tests"
      contains: "test_breusch_pagan_lm"
  key_links:
    - from: "tests/test_models.py::test_breusch_pagan_lm"
      to: "models/econometric.py::run_breusch_pagan_lm"
      via: "import + call against run_pooled_ols(full_panel) result"
      pattern: "run_breusch_pagan_lm"
    - from: "tests/test_models.py::test_delta_leverage_all"
      to: "models/econometric.py::run_delta_leverage_all"
      via: "direct call, asserts on hausman sub-dict"
      pattern: "result\\[.hausman.\\]"
    - from: "tests/test_models.py::test_delta_leverage_by_stage"
      to: "models/econometric.py::run_delta_leverage_by_stage"
      via: "iterates returned dict, distinguishes error path"
      pattern: "results\\[.Shakeout1.\\]"
---

<objective>
Harden the existing 4 delta-leverage / BP-LM unit tests in `tests/test_models.py` so that each Phase 1 success criterion (TST-01, TST-02, DLV-01..DLV-04) is verified by an explicit assertion, not just key-presence.

Purpose: Phase 1 backend functions in `models/econometric.py` already exist and pass loosely. The thesis gap-closure work is to lock the contract: the test suite must FAIL if a future refactor weakens any documented behaviour (verdict text, first-difference reduction, Hausman recommendation, stage error path).

Output: Updated `tests/test_models.py` with stricter assertions on `test_breusch_pagan_lm`, `test_delta_leverage_ols`, `test_delta_leverage_all`, and `test_delta_leverage_by_stage`. No changes to `models/econometric.py`. No new tests added — only assertion hardening on the existing 4.
</objective>

<execution_context>
@C:\Users\hemas\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\hemas\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-delta-leverage-diagnostics/01-RESEARCH.md
@tests/test_models.py
@tests/conftest.py
@models/econometric.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden test_breusch_pagan_lm and test_delta_leverage_ols assertions</name>
  <files>tests/test_models.py</files>
  <action>
Open `tests/test_models.py`. Locate the two tests at lines 70-85.

**Edit `test_breusch_pagan_lm` (line 70-78)** to lock TST-01/TST-02:
- Keep existing assertions (`lm_stat`, `lm_pvalue`, `lm_stat > 0`, `verdict` key).
- Add: `assert 0.0 <= bp["lm_pvalue"] <= 1.0` (p-value is a probability).
- Add: `assert isinstance(bp["verdict"], str) and len(bp["verdict"]) > 0` (TST-02 — test result has interpretation).
- Add: `assert "f_stat" in bp and "f_pvalue" in bp` (full contract per RESEARCH.md §"Return dict contract").
- Add: `assert bp["verdict"] in ("Pooled OLS", "Random Effects") or "RE" in bp["verdict"] or "Pooled" in bp["verdict"]` — verdict drives Pooled vs RE selection (TST-01). Use a tolerant check because the exact verdict string is set by `models/econometric.py::run_breusch_pagan_lm` and may include explanatory prefix.

**Edit `test_delta_leverage_ols` (line 80-85)** to lock DLV-01:
- Keep `assert result["type"] == "Pooled OLS"` and `assert result["n_obs"] > 3000`.
- Add: `from models.econometric import run_pooled_ols` and compute `baseline = run_pooled_ols(full_panel)`.
- Add: `assert result["n_obs"] < baseline["n_obs"]`, with comment `# DLV-01: first-difference drops first obs per firm`.
- Add: `assert "coef_table" in result and len(result["coef_table"]) > 0` (DLV-01 — coefficient table returned).
- Add: `assert result["n_obs"] > 0` (sanity — first-differencing did not destroy the panel).

DO NOT rename or restructure the tests. DO NOT touch `models/econometric.py`. DO NOT add new test functions — only add assertions inside the existing two tests.
  </action>
  <verify>
Run: `py -3.12 -m pytest tests/test_models.py::ModelsTest::test_breusch_pagan_lm tests/test_models.py::ModelsTest::test_delta_leverage_ols -v`

Both tests must PASS with the new assertions. If `test_breusch_pagan_lm` fails on the verdict-content check, inspect the actual verdict string returned by `run_breusch_pagan_lm` and adjust the tolerant `in` check to match real output (do not change the function — adapt the assertion to observed behaviour).
  </verify>
  <done>
Both tests pass. `test_breusch_pagan_lm` has at least 6 assertions (was 4). `test_delta_leverage_ols` has at least 5 assertions (was 2). Each Phase 1 requirement TST-01, TST-02, DLV-01 is anchored by at least one explicit assertion with an inline comment naming the requirement.
  </done>
</task>

<task type="auto">
  <name>Task 2: Harden test_delta_leverage_all and test_delta_leverage_by_stage assertions</name>
  <files>tests/test_models.py</files>
  <action>
Continue editing `tests/test_models.py`. Locate the two tests at lines 87-103.

**Edit `test_delta_leverage_all` (line 87-94)** to lock DLV-02:
- Keep `assert result["recommended"] in ("Fixed Effects", "Random Effects")`.
- Keep `assert "ols" in result`, `"fe" in result`, `"re" in result`.
- Add: `assert "hausman" in result` (DLV-02).
- Add: `h = result["hausman"]; assert "chi2" in h and "p_value" in h and "verdict" in h and "recommended" in h` (full Hausman contract per RESEARCH.md §"Return dict contract").
- Add: `assert h["chi2"] >= 0 and 0.0 <= h["p_value"] <= 1.0` (statistical sanity).
- Add: `assert h["recommended"] == result["recommended"]` (DLV-02 — Hausman drives top-level recommendation).
- Add: `assert result["fe"]["n_obs"] == result["re"]["n_obs"]` (delta panel is shared between FE and RE).

**Edit `test_delta_leverage_by_stage` (line 96-103)** to lock DLV-03/DLV-04:
- Keep `assert len(results) >= 3`.
- Replace the loose `for stage, res in results.items(): if "error" not in res: assert "coef_table" in res` with stricter logic:
  - Track stages with results vs stages with errors: `ok = [s for s,r in results.items() if "error" not in r]; errors = [s for s,r in results.items() if "error" in r]`.
  - `assert len(ok) >= 1, "At least one stage must produce a coef_table (DLV-03)"`.
  - For each ok stage assert: `"coef_table" in res`, `"n_obs" in res and res["n_obs"] > 0`, `"r_squared" in res` (DLV-04 — coefficient comparison data present).
  - For each error stage assert: `isinstance(res["error"], str) and "Too few observations" in res["error"]` (RESEARCH.md §"Return dict contract" — documented error string).
- Add: `assert "Shakeout1" in errors`, with comment `# RESEARCH.md: Shakeout1 has 31 obs; first-differencing drops it below the n<30 guard (documented behaviour, not a bug)`. If this assertion fails, change to a soft warning: `assert "Shakeout1" in errors or "Shakeout1" not in results, f"Unexpected Shakeout1 state: {results.get('Shakeout1')}"` — the planning context says this is the expected error path; verify before relaxing.

DO NOT modify `models/econometric.py`. DO NOT add new test functions. DO NOT rename existing test methods.
  </action>
  <verify>
Run: `py -3.12 -m pytest tests/test_models.py::ModelsTest::test_delta_leverage_all tests/test_models.py::ModelsTest::test_delta_leverage_by_stage -v`

Both must PASS. Then run the full models test file to confirm no regressions: `py -3.12 -m pytest tests/test_models.py -v`. All 40 tests must pass.

If the Shakeout1 assertion is brittle (the actual stage list differs from RESEARCH.md), drop to the tolerant form documented in the action — but log the actual `results.keys()` and `errors` lists in the SUMMARY so future refactors know the real behaviour.
  </verify>
  <done>
Both tests pass with hardened assertions. `test_delta_leverage_all` has at least 9 assertions covering OLS+FE+RE+Hausman+recommendation linkage. `test_delta_leverage_by_stage` partitions results into ok/error stages, asserts coef_table contract on ok stages and documented error string on error stages, and verifies the Shakeout1 small-sample edge case. All 40 tests in `tests/test_models.py` still pass. No changes to `models/econometric.py`.
  </done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# Targeted: hardened tests
py -3.12 -m pytest tests/test_models.py::ModelsTest::test_breusch_pagan_lm tests/test_models.py::ModelsTest::test_delta_leverage_ols tests/test_models.py::ModelsTest::test_delta_leverage_all tests/test_models.py::ModelsTest::test_delta_leverage_by_stage -v

# Full test_models.py (40 tests, ~38s baseline per RESEARCH.md)
py -3.12 -m pytest tests/test_models.py -v

# Diff check — no production code touched
git diff --stat models/
# Expected: no changes to models/
```

All 4 hardened tests pass. All 40 tests in test_models.py pass. No diff in `models/`.
</verification>

<success_criteria>
- [ ] TST-01 (BP-LM Pooled vs RE) asserted by content of `verdict` field
- [ ] TST-02 (BP-LM result with interpretation) asserted by chi-sq, p-value, and non-empty verdict string
- [ ] DLV-01 (delta-leverage OLS/FE/RE) asserted by `n_obs < baseline_n_obs` and presence of coef_table
- [ ] DLV-02 (Hausman on delta-leverage) asserted by hausman.recommended == result.recommended linkage
- [ ] DLV-03 (stage-specific regressions) asserted by at least one stage with coef_table
- [ ] DLV-04 (coefficient comparison) asserted by coef_table + n_obs + r_squared on every ok stage
- [ ] Documented error path (Shakeout1 < 30 obs) verified by error-string assertion
- [ ] All 40 tests in `tests/test_models.py` pass
- [ ] Zero changes to `models/econometric.py`
</success_criteria>

<output>
After completion, create `.planning/phases/01-delta-leverage-diagnostics/01-01-SUMMARY.md` capturing:
- Final assertion count per test (before vs after)
- Actual `verdict` string returned by `run_breusch_pagan_lm` (for future reference)
- Actual `results.keys()` and which stages errored in `run_delta_leverage_by_stage`
- Confirmation that all 40 tests in `test_models.py` pass
</output>
