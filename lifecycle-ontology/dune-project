(lang dune 3.16)
(name lifecycle_ontology)

; ── Package definition (generates lifecycle_ontology.opam) ─────────────────
(generate_opam_files true)
(package
 (name lifecycle_ontology)
 (synopsis "Lifecycle leverage — OCaml ontology and analytics meta-layer")
 (description
  "Semantic and analytics meta-layer for the LifeCycle Leverage platform.
   Provides domain types (stage, period, metric, company), analytics artefacts
   (model, statistic, normative_band, scenario), graph export (Macro/Meso/Micro),
   and HTTP/MCP services.")
 (depends
  (ocaml (>= 5.2.0))
  dune
  (eio_main (>= 1.0))
  (yojson (>= 2.1))
  (ocamlgraph (>= 2.1))
  (cmdliner (>= 1.3))
  (dream (>= 1.0))
  (alcotest (and :with-test (>= 1.7)))))
