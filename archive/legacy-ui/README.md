# Archived legacy UI

The former workflow that launched a page file directly (for example,
`pages/19_ai_assistant.py`) is archived and unsupported. It bypasses the canonical
`app.py` shell and creates Streamlit's legacy auto-discovered navigation.

Use the root application only:

`http://localhost:8501/`

This directory is a marker, not an active application entrypoint.
