[tool.ruff]
target-version = "py311"
line-length = 120
exclude = [".venv", "build", "dist", "migrations"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.pytest.ini_options]
markers = [
    "ui: optional authenticated browser checks for the Streamlit UI",
]
